Service for relaying 433/315MHz LPD/SRD signals with generic low-cost GPIO RF modules on a Raspberry Pi.

## Instalation
```
sudo cp systemd/mqtt2rf.service /lib/systemd/system/
sudo systemctl enable mqtt2rf.service
```
